# coding: utf-8
Y_data.columns
Y_data[unitid]
Y_data['unitid']
temp = years_are_contiguous(Y_data)
temp
print(get_year_distrib(Y_data))
id2year
Y_data
Y['unitid'
]
Y_data['unitid']
Y_data.sort(['unitid'], inplace=True, kind='heapsort')
Y_data
Y_data['unitid']
Y_data.sort(['unitid'], inplace=True, kind='heapsort')
Y_data.sort_values(['unitid'], inplace=True, kind='heapsort')
get_ipython().magic(u'paste')
same_schools(X_data, Y_data)
temp = same_schools(X_data, Y_data)
get_ipython().magic(u'paste')
temp = same_schools(X_data, Y_data)
temp
len(temp)
get_ipython().magic(u'paste')
temp = same_schools(X_data, Y_data)
len(temp)
get_ipython().magic(u'paste')
temp = same_schools(X_data, Y_data)
temp
len(temp)
get_ipython().magic(u'paste')
temp = same_schools(X_data, Y_data)
len(temp)
temp = same_schools(X_data, Y_data, contiguous=False)
len(temp)
X_data[X['unitid'] in { 342, 43243, 54353 }]
X_data[X_data['unitid'] in { 342, 43243, 54353 }]
x = {1, 2, 3}
X_data[X_data['unitid'] in x]
X_data[X_data['unitid'] in list(x)]
X_data[X_data['unitid'] == y for y in x]
X_data[X_data['unitid'] == 123]
X_data['unitid_linchpin']
X_data['unitid']
X_data['unitid_linchpin']
X_data['unitid']
X_data[X_data['unitid'].isin(x)]
x
complex(2)
return fail
def fail():
    return fail
fail()
del fail
def hahaha():
    return fail
hahaha()
def hahaha():
    return fail;
hahaha()
get_ipython().magic(u'clear ')
X_data
Y_data
get_ipython().magic(u'paste')
X_test, Y_test = create_test(X_data, Y_data, contiguous=False)
get_ipython().magic(u'clear ')
x = [1,2,3,4]
x[:2]
get_ipython().magic(u'paste')
X_test, Y_test = create_test(X_data, Y_data, contiguous=False)
get_ipython().magic(u'paste')
X_test, Y_test = create_test(X_data, Y_data, contiguous=False)
X_test, Y_test = create_test(X_data, Y_data, 0.1, contiguous=False)
X_data[not X['unitid'].isin(x)]
X_data[not X_data['unitid'].isin(x)]
X_data[not bool(x_data['unitid'].isin(x))]
X_data[not bool(X_data['unitid'].isin(x))]
X_data[bool(not X_data['unitid'].isin(x))]
get_ipython().magic(u'paste')
X_test, Y_test, X_train, Y_train = create_test(X_data, Y_data, 0.1, contiguous=False)
import random
X_test, Y_test, X_train, Y_train = create_test(X_data, Y_data, 0.1, contiguous=False)
len(X_test)
len(Y_test)
len(X_train)
len(Y_train)
len(X_data)
len(Y_data)
len(X_test) + len(X_train)
x = [0, 1, 2, 3, 4, 5]
x[:3]
x[3:]
set(X_test['unitid']) & set(X_train['unitid'])
set(X_data['unitid']) == set(X_test['unitid']) | set(X_train['unitid'])
set(X_data['unitid']) ^ ( set(X_test['unitid']) | set(X_train['unitid']))
len(set(X_data['unitid']) ^ ( set(X_test['unitid']) | set(X_train['unitid'])))
len(X_test) + len(X_train)
len(X_test) + len(X_train) + len(set(X_data['unitid']) ^ ( set(X_test['unitid']) | set(X_train['unitid'])))
len(X_data)
get_ipython().magic(u'paste')
X_test, Y_test, X_train, Y_train = create_test(X_data, Y_data, 0.1, contiguous=False)
len(X_test) + len(X_train) == len(X_data)
x = list(range(10000000))
get_ipython().magic(u'timeit map(lambda y: y ** 2, x)')
get_ipython().magic(u'timeit [y ** 2 for y in x]')
